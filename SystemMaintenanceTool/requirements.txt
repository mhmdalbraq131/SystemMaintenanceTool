# Runtime dependencies only. Tkinter is included with standard Windows Python.
psutil>=5.9.0
pywin32>=305; sys_platform == "win32"
WMI>=1.5.1; sys_platform == "win32"

# Optional development/build tools are intentionally not required at runtime.
